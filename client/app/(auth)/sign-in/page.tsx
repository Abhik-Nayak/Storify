"use client"

import AuthForm from '@/components/AuthForm';
import React, { useState } from 'react';

type Props = {};

const Signin = (props: Props) => <AuthForm type="sign-in" />

export default Signin;
