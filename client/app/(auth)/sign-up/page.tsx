"use client";

import AuthForm from '@/components/AuthForm';
import React, { useState } from 'react';

type Props = {};

const Signup = (props: Props) => <AuthForm type="sign-up" />

export default Signup;
